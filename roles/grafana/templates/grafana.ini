[server]
protocol = http
http_port = {{ grafana_container_port }}
domain = {{ bind_domain }}
root_url = %(protocol)s://%(domain)s:%(http_port)s/grafana/

[security]
admin_user = {{ grafana_user }}
admin_password = {{ grafana_password }}
